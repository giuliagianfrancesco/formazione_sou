Lo script esegue prima un comando corretto (stampa "hello") e stampa l'exit code corrispondente (0).

Esegue poi un comando non valido e stampa l'exit code (diverso da zero perchè segnale un errore). infine ritorna un valore 113 come exit code per segnalaere errore nell'esecuzione.
